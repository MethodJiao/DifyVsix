<UserControl x:Class="DifyVsix.ToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:vsPlatformUI="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="{DynamicResource {x:Static vsPlatformUI:EnvironmentColors.ToolWindowBackgroundBrushKey}}"
             Foreground="{DynamicResource {x:Static vsPlatformUI:EnvironmentColors.ToolWindowTextBrushKey}}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow" Loaded="MyToolWindow_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="0"/> <!--原本是150-->
        </Grid.RowDefinitions>
        <Border Grid.Row="0">
            <!--<md:MarkdownScrollViewer x:Name="mdview" Margin ="10" Markdown="{Binding MarkdownContent, Mode=OneWay}" KeyDown="inputText_KeyDown">
                <md:MarkdownScrollViewer.MarkdownStyle>
                    <Style TargetType="FlowDocument" BasedOn="{x:Static md:MarkdownStyle.Sasabune}">
                    </Style>
                </md:MarkdownScrollViewer.MarkdownStyle>
            </md:MarkdownScrollViewer>-->
            <wv2:WebView2 x:Name="mdview" />
        </Border>
        <Border Grid.Row="1">
            <StackPanel Margin="5,5,5,5">
                <TextBox x:Name="inputText" TextWrapping="Wrap" Text="" Height="120" Background="{DynamicResource {x:Static vsPlatformUI:EnvironmentColors.ToolWindowBackgroundBrushKey}}" Foreground="{DynamicResource {x:Static vsPlatformUI:EnvironmentColors.ToolWindowTextBrushKey}}" KeyDown="inputText_KeyDown"/>
                <Button Content="发送" Click="buttonSend_Click" x:Name="buttonSend" Grid.RowSpan="2" Height="20" Width="50" VerticalAlignment="Bottom" HorizontalAlignment="Right" Background="{DynamicResource {x:Static vsPlatformUI:EnvironmentColors.ToolWindowBackgroundBrushKey}}" Foreground="{DynamicResource {x:Static vsPlatformUI:EnvironmentColors.ToolWindowTextBrushKey}}"/>
            </StackPanel>
           
        </Border>
        


    </Grid>
</UserControl>
